# 00 走进终端 (Introduction)

欢迎来到 Linux 的世界！在开始学习命令之前，我们需要先了解几个核心概念。

## 1. 什么是终端 (Terminal) 与 Shell？
- **终端 (Terminal)**：是你输入命令、查看输出的那个“窗口”。比如 Ubuntu 上的 `Gnome Terminal`，KDE 上的 `Konsole`，MacOS 的 `iTerm2`。
- **Shell**：是窗口背后的“解析器”，它负责解析你输入的指令并传达给操作系统内核。
  - **Bash**：绝大多数 Linux 发行版的默认 Shell。
  - **Zsh**：现代化的 Shell，功能更强（本教程中的很多“Pro Tip”配合 Zsh 效果最佳）。

## 2. 你在屏幕上看到了什么？(理解提示符)
当你打开终端，你会看到类似这样的一行字：
`han@ubuntu:~/Documents$ `
- **han**：当前登录的用户名。
- **ubuntu**：计算机的名字 (Hostname)。
- **~/Documents**：你当前所在的文件夹路径 (`~` 代表家目录)。
- **$**：普通用户提示符 (如果是 **#** 则代表你是 root 管理员)。

## 3. 核心准则：Linux 是区分大小写的
在 Windows 中，`File.txt` 和 `file.txt` 是同一个文件；但在 Linux 中，它们是**两个完全不同**的文件。命令也是一样，`ls` 有效，但 `LS` 会报错。

## 4. 授人以渔：如何获取帮助？
遇到不认识的命令？不要只靠百度，学会使用 Linux 自带的“说明书”：
- **`命令 --help`**：查看简要的参数说明 (最常用)。
- **`man 命令`**：查看详尽的开发手册 (Manual)。按 `q` 退出。
- **`tldr 命令`**：(需安装) Too Long; Didn't Read。它是 `man` 的简化版，只给你看最常用的例子，非常适合新手。

## 5. 如何安装工具？
根据你的系统，使用对应的包管理器：
- **Ubuntu/Debian**: `sudo apt update && sudo apt install 工具名`
- **CentOS/RHEL/Fedora**: `sudo dnf install 工具名` (旧版使用 `yum`)
- **Arch Linux**: `sudo pacman -S 工具名`
- **MacOS**: `brew install 工具名` (需安装 Homebrew)

### 💡 磨刀不误砍柴工：预装现代化工具
为了获得最佳学习体验，本教程会推荐一些比传统命令更好用的“现代化替代品”（如 `fzf`, `zoxide`, `bat`, `ripgrep`）。
> 🔗 **详细安装指南**：请查看实战篇的 [现代化工具](../04_实战/01_现代化工具.md)。

## 6. 学习路线图
本教程分为五个阶段：

1. **基础篇**：
   - 文件管理 (`ls`, `cp`, `mv`)
   - 查看与编辑 (`cat`, `vim`, `nano`)
   - 权限与用户 (`chmod`, `sudo`)
   - 终端效率与重定向 (快捷键, `|`, `>`)

2. **核心篇**：
   - 查找与搜索 (`find`, `grep`)
   - 文本处理与正则 (`sed`, `awk`, Regex)
   - 环境变量 (`PATH`, `.bashrc`)

3. **进阶篇**：
   - 进程管理 (`ps`, `top`)
   - 网络与远程 (`ssh`, `curl`)
   - 磁盘与归档 (`tar`, `du`)
   - Shell 脚本编程 (`for`, `if`)
   - 系统自动化 (`cron`, `systemd`)

4. **实战篇**：
   - 现代化工具、Git、Docker、避坑指南、速查手册。

5. **高级专题**：
   - 系统构建与部署、内核原理与调试、网络底层与安全。

---
**现在，让我们从最基础的文件管理开始，迈出第一步！**
