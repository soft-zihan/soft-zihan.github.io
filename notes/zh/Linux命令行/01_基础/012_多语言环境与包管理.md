# 📦 多语言环境与包管理完全指南

## 📑 目录大纲

1.  **⚖️ 选型决策：经典标准 vs 现代极速** (一分钟速查)
2.  **🐍 Python：通用开发与 AI**
    *   2.1 **官方标准**：Pip & Venv (服务器/教程通用)
    *   2.2 **现代推荐**：uv (极速全能)
    *   2.3 **AI 专用**：Conda / Mamba
3.  **🟢 Node.js：前端生态**
    *   3.1 **版本管理**：nvm (经典) vs fnm (极速)
    *   3.2 **包管理**：npm (官方基石) vs pnpm (省空间神器)
4.  **☕ Java：后端生态**
    *   4.1 **版本管理**：SDKMAN!
    *   4.2 **构建工具**：Maven & Gradle
5.  **🌐 全能统领：mise** (多语言一站式管理)
6.  **🐧 Linux：系统底层包管理** (运维排查)

---

## 1. ⚖️ 选型决策：经典标准 vs 现代极速

| 领域 | 🐢 **经典标准 (稳、兼容性好)** | 🐇 **现代极速 (快、体验好)** | **💡 建议** |
| :--- | :--- | :--- | :--- |
| **Node 版本** | **nvm** (Shell 脚本，启动稍慢) | **fnm** (Rust 编写，跨平台极快) | 个人用 **fnm**，团队统一配置两者都兼容。 |
| **Node 包管理** | **npm** (Node 自带，平铺依赖) | **pnpm** (硬链接机制，省硬盘) | 新项目无脑选 **pnpm**；新手先学 **npm**。 |
| **Python 管理** | **pip + venv** (系统内置) | **uv** (秒级安装，管理一切) | 生产环境/老项目用 **pip**；个人开发用 **uv**。 |
| **AI/数据** | **Conda** (大而全) | **Mamba** (Conda 的 C++ 重写版) | 必须用 **Mamba**，否则解析依赖会等到死。 |
| **Java** | 手动配置环境变量 | **SDKMAN!** | 必须用 **SDKMAN!**，手动配环境是折磨。 |

---

## 2. 🐍 Python：通用开发与 AI

### 2.1 🐢 官方标准：Pip & Venv
> **定位**：Python 内置，无需安装任何第三方工具。所有 Linux 服务器和教程的默认标准。

```bash
# 1. 创建虚拟环境 (在项目目录下生成 .venv 文件夹)
python -m venv .venv

# 2. 激活环境
# Mac/Linux:
source .venv/bin/activate
# Windows:
.venv\Scripts\activate

# 3. 安装依赖 (使用 requirements.txt)
pip install -r requirements.txt
# 或者单独安装
pip install requests

# 4. 导出当前环境依赖
pip freeze > requirements.txt
```

### 2.2 🐇 现代推荐：uv
> **定位**：Rust 编写的终极工具。它一个工具替代了 `pip` (装包)、`venv` (环境)、`pyenv` (下载 Python)。

```bash
# 1. 初始化项目 (自动创建虚拟环境和 pyproject.toml)
uv init --python 3.11

# 2. 安装依赖 (极速并发下载)
uv add requests
uv add --dev pytest  # 开发依赖

# 3. 锁定依赖 (Lock) - 生产环境必备
# uv 会自动生成 uv.lock 文件，确保生产环境安装的版本和开发环境完全一致
uv sync

# 4. 导出为 requirements.txt (为了兼容老系统)
uv pip compile pyproject.toml -o requirements.txt

# 5. 运行命令 (隐式激活，无需手动 source)
uv run main.py
```

### 2.3 🧠 AI 专用：Conda / Mamba
> **定位**：解决 PyTorch/TensorFlow 等包含 C/C++ 库的复杂依赖。请务必使用 **Mamba** 替代 Conda。

```bash
# 1. 创建环境
mamba create -n ai_env python=3.10

# 2. 激活环境
mamba activate ai_env

# 3. 安装 PyTorch (走 conda-forge 源，无需编译)
mamba install pytorch torchvision -c pytorch
```

---

## 3. 🟢 Node.js：前端生态

前端分为两部分：管 Node 版本的（Runtime）和管第三方库的（Libs）。

### 3.1 版本管理：Runtime
**痛点**：老项目要 Node 14，新项目要 Node 20。

#### 🅰️ 经典方案：nvm (Node Version Manager)
工业界使用最广，基于 Shell 脚本。

```bash
# 1. 安装最新版
nvm install node

# 2. 安装特定版本
nvm install 18.17.0

# 3. 切换版本
nvm use 18

# 4. 设置默认版本 (新开终端时生效)
nvm alias default 18
```

#### 🅱️ 极速方案：fnm (Fast Node Manager)
Rust 编写，跨平台（支持 Windows），无启动延迟。

```bash
# 1. 安装并自动切换
fnm use --install-if-missing 20

# 2. 自动解析 (进入含 .nvmrc 的目录自动切换)
fnm env --use-on-cd | source
```

### 3.2 包管理与构建：Libraries & Build
**痛点**：`node_modules` 体积太大（黑洞），构建命令容易忘。

#### 🅰️ 官方基石：npm
Node.js 自带，初学者必须掌握的基础。

```bash
# 1. 初始化 (生成 package.json)
npm init -y

# 2. 安装依赖
npm install axios          # 生产依赖 (dependencies)
npm install -D typescript  # 开发依赖 (devDependencies)
npm install -g pm2         # 全局安装 (工具类)

# 3. 运行脚本 (package.json 中 "scripts" 定义的命令)
npm run dev     # 启动开发服务器
npm run build   # 📦 打包构建 (通常生成 dist/ 目录)
npm run test    # 运行测试
npm start       # 启动生产环境 (等同于 npm run start)

# 4. 换源 (加速下载)
npm config set registry https://registry.npmmirror.com
```

#### 🅱️ 效率首选：pnpm
利用**硬链接**技术，全电脑只存一份包文件，节省几十 GB 空间。

```bash
# 1. 安装 pnpm
npm install -g pnpm

# 2. 安装依赖 (命令与 npm 高度相似)
pnpm install       # 安装所有依赖
pnpm add axios     # 安装生产依赖
pnpm add -D vite   # 安装开发依赖

# 3. 构建与运行
pnpm dev
pnpm build         # 📦 执行构建
```

---

## 4. ☕ Java：后端生态

### 4.1 版本总管：SDKMAN!
> **定位**：不仅管理 JDK，还能管理 Maven, Gradle, Spring Boot CLI。Java 开发者的必备神器。

```bash
# 1. 列出可用的 Java 版本 (Temurin, Oracle, GraalVM 等)
sdk list java

# 2. 安装指定版本
sdk install java 21.0.1-tem

# 3. 设置默认版本
sdk default java 17.0.9-tem

# 4. 切换到项目所需版本
sdk use java 8.0.392-tem
```

### 4.2 构建工具
> **定位**：负责编译、测试、打包 (.jar)。

*   **Maven (经典)**：XML 配置，结构严谨，企业级首选。
    *   `mvn clean package`: **清理并打包** (生成 target/xxx.jar)。
    *   `mvn install`: 打包并安装到本地仓库。
    *   `mvn dependency:tree`: 查看依赖树（排查冲突）。
*   **Gradle (灵活)**：Groovy/Kotlin 配置，Android 标配。
    *   `gradle build`: **执行完整构建** (包含测试和打包)。
    *   `gradle bootRun`: (Spring Boot) 直接启动应用。

---

## 5. 🌐 全能统领：mise (原 rtx)

> **场景**：全栈开发。你不想分别安装 nvm, pyenv, sdkman，只想用一个工具管所有语言。

*   **原理**：通过项目目录下的 `.mise.toml` 自动配置环境变量。
*   **兼容性**：它能读懂 `.nvmrc` 和 `.java-version`。

```bash
# 1. 安装多种语言工具
mise use node@20
mise use python@3.12
mise use java@temurin-17

# 2. 查看当前目录生效的工具
mise ls

# 3. 全局配置 (类似安装在系统层)
mise use -g node@lts
```

---

## 6. 🐧 Linux：系统底层包管理 (进阶)

当上述语言级工具失效，或者需要安装 Nginx, Docker, Git 等系统软件时使用。

### 6.1 APT (Debian/Ubuntu) 核心速查
```bash
# 搜索软件
apt search nginx

# 安装/卸载
sudo apt install nginx
sudo apt remove nginx      # 卸载软件 (保留配置文件)
sudo apt purge nginx       # 🔥 彻底卸载 (删除软件 + 配置文件)

# 查找某个文件属于哪个包 (编译报错缺头文件时用)
# 需先安装: sudo apt install apt-file && sudo apt-file update
apt-file search stdio.h

# 锁定版本 (防止系统升级时自动更新该软件)
sudo apt-mark hold docker-ce

# 自动修复安装 (依赖损坏时用)
sudo apt install -f
```

### 6.2 DPKG (Debian/Ubuntu 底层)
```bash
# 查看已安装包的详细列表
dpkg -l | grep nginx

# 查看软件安装到了哪些目录 (找配置文件位置)
dpkg -L nginx-full

# 彻底卸载 (连同配置文件)
sudo dpkg -P nginx
```

### 6.3 Homebrew (Linux 版)
**场景**：你没有服务器的 root 权限（sudo），但需要安装最新版的 git 或 node。
```bash
# 安装软件到用户主目录，无需 root
brew install gcc node
```