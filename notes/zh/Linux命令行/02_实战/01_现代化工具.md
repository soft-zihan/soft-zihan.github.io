# 🌟 现代化工具与技巧

> 2024年推荐安装的 Rust/Go 替代工具，提升 10 倍幸福感。

## 1. 现代化替代方案 (Modern Replacements)

| 经典命令 | 现代化替代 (推荐安装) | 优势 |
| :--- | :--- | :--- |
| `man` | **`tldr`** | **最强推荐**：不再显示晦涩的文档，只显示最常用的几个例子。 |
| `ls` | **`exa`** (或 `eza`) | 支持 Git 状态显示、图标、树状视图 (`exa --tree`) |
| `cat` | **`bat`** | 支持语法高亮、自动分页、显示行号、Git 变更标记 |
| `find` | **`fd`** | 语法更简单 (`fd pattern`)，默认忽略 `.gitignore`，速度极快 |
| `grep` | **`rg`** (Ripgrep) | 搜索速度最快、智能过滤垃圾文件 |
| `cd` | **`z`** / **`zoxide`** | 智能跳转，根据访问频率直接跳到目录 (如 `z pro` -> `/home/user/projects`) |
| `du` | **`dust`** | 类似 `ncdu` 但更直观的树状磁盘占用图 |
| `diff` | **`delta`** | 极美观的 Git diff 查看器，支持语法高亮 |
| `top` | **`btop`** / `glances` | 比 `htop` 更酷炫，支持鼠标，显示更多硬件信息 |
| `curl` | **`httpie`** (http) | 输出带高亮的 JSON，语法更人性化 |

## 2. 深度组合技巧 (The Power Combo)

工具本身很强，但**组合起来**才是真正的效率怪兽。

### 🚀 fzf：模糊搜索之王
不要只把它当成一个搜索框，它是命令行的“任意门”。
- **历史命令秒搜**：
  在终端按 `Ctrl + R`。配合 `fzf`，你可以输入命令中的任意片段（甚至不按顺序），瞬间定位到几个月前写过的复杂长命令。
- **文件交互搜索**：
  `vim $(fzf)`：直接在模糊搜索列表中选文件并用 Vim 打开。
- **与进程管理结合**：
  `kill -9 $(ps -ef | fzf | awk '{print $2}')`：交互式选择并杀死进程。

### ⚡ zoxide：比 cd 聪明 10 倍
它是 `cd` 的现代化替代品。
- **记忆跳转**：它会记住你访问过的目录。
- **模糊匹配**：你只需输入 `z proj`，它就能直接跳到 `/home/user/work/my-super-project`。
- **快速回退**：`z -` 跳回上一个目录。

---

## 4. 进阶配置与技巧

### 🚀 fzf 预览功能 (Preview)
默认的 fzf 只是个列表，配合 `bat` 可以实现**实时预览**文件内容。
- **配置**：在 `.bashrc` 或 `.zshrc` 中添加：
  ```bash
  export FZF_DEFAULT_OPTS="--preview 'bat --style=numbers --color=always --line-range :500 {}'"
  ```
- **效果**：当你运行 `vim $(fzf)` 时，右侧会出现一个窗口，实时显示选中文件的内容。

### ⚙️ Ripgrep (rg) 配置文件
每次都打 `--hidden --glob '!node_modules'` 很累？你可以把默认参数写进配置文件。
1. 创建文件 `~/.ripgreprc`：
   ```text
   --hidden
   --glob=!node_modules/*
   --glob=!.git/*
   --smart-case
   ```
2. 在 `.bashrc` 中生效：
   ```bash
   export RIPGREP_CONFIG_PATH="$HOME/.ripgreprc"
   ```

### ⚡ zoxide 深度集成
- **`zi`**：使用 `fzf` 交互式选择跳转目录。
  - *场景*：你只记得目录里有个 "config"，输入 `zi`，在列表里搜 "config" 并直接跳转。

---

## 5. 为什么选择现代化工具？
- **速度更快**：许多工具使用 Rust 编写（如 `rg`, `fd`, `bat`），处理大型项目时优势巨大。
- **默认即巅峰**：无需像老工具那样背诵几十个参数，默认输出就有语法高亮和 Git 集成。
- **降低心智负担**：比如 `fd` 默认就会帮你跳过 `node_modules` 和 `.git`，让你专注于业务文件。

> **安装建议**：
> - **Ubuntu/Debian**: `sudo apt install fzf zoxide bat ripgrep fd-find`
> - **推荐别名**：在 `.zshrc` 或 `.bashrc` 中设置 `alias cat='bat'` 和 `alias cd='z'`。
