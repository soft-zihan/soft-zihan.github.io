# 模板语法与插值

> **对应实验室**: LabHtml, LabDirectives  
> **涉及源文件**: App.vue, components/*.vue

## 1. 什么是 Vue 模板？

Vue 使用基于 HTML 的模板语法，允许你声明式地将组件数据绑定到 DOM。

```vue
<template>
  <!-- 这里是模板区域 -->
  <div>{{ message }}</div>
</template>

<script setup>
const message = '你好，樱花笔记！'
</script>
```

## 2. 文本插值 {{ }}

最基础的数据绑定形式是"Mustache"语法（双花括号）：

```vue
<template>
  <!-- 📍 简单插值 -->
  <span>消息: {{ msg }}</span>
  
  <!-- 📍 表达式（可以进行计算） -->
  <span>{{ count + 1 }}</span>
  <span>{{ ok ? '是' : '否' }}</span>
  <span>{{ message.split('').reverse().join('') }}</span>
</template>
```

### 本站实例

```vue
<!-- 📍 AppHeader.vue -->
<h1>{{ t.welcome_title }}</h1>
<!-- t 是计算属性，根据当前语言返回对应翻译 -->

<!-- 📍 ArticleCard.vue -->
<span>{{ article.wordCount }} {{ t.words }}</span>
```

## 3. 原始 HTML（v-html）

双花括号会将数据解释为纯文本。若要输出真正的 HTML，需要使用 `v-html`：

```vue
<template>
  <p>文本插值: {{ rawHtml }}</p>
  <!-- 输出: <span style="color: red">红色文字</span> -->
  
  <p>HTML渲染: <span v-html="rawHtml"></span></p>
  <!-- 输出: 红色文字（真正的红色） -->
</template>

<script setup>
const rawHtml = '<span style="color: red">红色文字</span>'
</script>
```

> ⚠️ **安全警告**：只对可信内容使用 v-html，永远不要对用户输入使用！

### 本站实例

```vue
<!-- 📍 渲染 Markdown 转换后的 HTML -->
<article v-html="renderedContent"></article>
```

## 4. 属性绑定（v-bind）

双花括号不能用在 HTML 属性中，要用 `v-bind`：

```vue
<template>
  <!-- 完整语法 -->
  <img v-bind:src="imageSrc" />
  
  <!-- 缩写（推荐） -->
  <img :src="imageSrc" />
  
  <!-- 动态 class -->
  <div :class="{ active: isActive }">...</div>
  
  <!-- 动态 style -->
  <div :style="{ color: textColor }">...</div>
</template>
```

### 本站实例

```vue
<!-- 📍 App.vue - 主题切换 -->
<div :class="{ dark: isDark }">
  <!-- isDark 为 true 时添加 dark 类 -->
</div>

<!-- 📍 FileTree.vue - 动态图标 -->
<span :class="node.type === 'directory' ? 'folder-icon' : 'file-icon'">
```

## 5. 布尔属性

布尔属性根据真/假值来决定是否存在：

```vue
<template>
  <!-- disabled 属性 -->
  <button :disabled="isLoading">提交</button>
  
  <!-- 当 isLoading 为 true 时：<button disabled>提交</button> -->
  <!-- 当 isLoading 为 false 时：<button>提交</button> -->
</template>
```

### 本站实例

```vue
<!-- 📍 WriteEditor.vue -->
<button :disabled="!canPublish">
  {{ t.write_publish }}
</button>
```

## 6. 多属性绑定

可以一次绑定多个属性：

```vue
<template>
  <div v-bind="objectOfAttrs"></div>
</template>

<script setup>
const objectOfAttrs = {
  id: 'container',
  class: 'wrapper',
  style: 'color: red'
}
// 等同于 <div id="container" class="wrapper" style="color: red">
</script>
```

## 7. 表达式限制

模板中只能使用**单一表达式**：

```vue
<template>
  <!-- ✅ 正确 -->
  {{ number + 1 }}
  {{ ok ? 'YES' : 'NO' }}
  {{ message.split('').reverse().join('') }}
  
  <!-- ❌ 错误：这是语句，不是表达式 -->
  {{ var a = 1 }}
  
  <!-- ❌ 错误：流程控制也不行，用三元表达式 -->
  {{ if (ok) { return message } }}
</template>
```

## 8. 调用函数

可以在绑定中调用组件暴露的方法：

```vue
<template>
  <span :title="formatDate(date)">
    {{ formatDate(date) }}
  </span>
</template>

<script setup>
import { formatDate } from './utils'
// formatDate 可以在模板中直接使用
</script>
```

---

## 🧪 动手实验

本章节对应实验室：**LabHtml**, **LabDirectives**

完成以下练习：
1. [ ] 使用文本插值显示动态数据
2. [ ] 使用 `:class` 实现条件样式
3. [ ] 理解 `v-html` 的安全风险

---

## 📝 小结

| 语法 | 用途 | 示例 |
|------|------|------|
| `{{ }}` | 文本插值 | `{{ message }}` |
| `v-html` | 渲染 HTML | `<span v-html="html">` |
| `v-bind:` / `:` | 属性绑定 | `:src="url"` |
| `:class` | 动态类名 | `:class="{ active: isActive }"` |
| `:style` | 动态样式 | `:style="{ color: textColor }"` |
