# 项目结构解析

> **对应实验室**: LabCodeEvolution, LabBuildTools  
> **涉及源文件**: 整个项目根目录

## 1. Vue 3 项目整体结构

Sakura Notes 是一个典型的 Vue 3 + Vite 项目：

```
soft-zihan.github.io/
├── index.html          # 入口 HTML
├── App.vue             # 根组件
├── index.tsx           # 应用入口（挂载点）
├── vite.config.ts      # Vite 配置
├── tsconfig.json       # TypeScript 配置
├── package.json        # 依赖管理
├── constants.ts        # 全局常量（i18n）
├── types.ts            # 类型定义
│
├── components/         # 组件目录
│   ├── AppHeader.vue
│   ├── AppSidebar.vue
│   ├── lab/           # 实验室组件
│   └── petal/         # 花瓣动画
│
├── composables/        # 组合式函数
│   ├── useSearch.ts
│   ├── useFile.ts
│   └── ...
│
├── stores/             # Pinia 状态管理
│   ├── appStore.ts
│   ├── musicStore.ts
│   └── index.ts
│
├── notes/              # Markdown 笔记
│   ├── zh/
│   └── en/
│
└── public/             # 静态资源
    ├── image/
    ├── music/
    └── raw/           # 源码文件（供实验室查看）
```

## 2. 关键文件详解

### 2.1 index.html - 入口页面

```html
<!-- 📍 index.html -->
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/favicon.ico" />
    <title>Sakura Notes</title>
  </head>
  <body>
    <!-- Vue 应用挂载点 -->
    <div id="app"></div>
    
    <!-- Vite 会自动注入构建后的 JS -->
    <script type="module" src="/index.tsx"></script>
  </body>
</html>
```

### 2.2 index.tsx - 应用入口

```typescript
// 📍 index.tsx
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import './index.css'

// 创建 Vue 应用实例
const app = createApp(App)

// 注册 Pinia 状态管理
app.use(createPinia())

// 挂载到 DOM
app.mount('#app')
```

### 2.3 App.vue - 根组件

```vue
<!-- 📍 App.vue 简化结构 -->
<template>
  <div :class="{ dark: isDark }">
    <!-- 背景层 -->
    <WallpaperLayer />
    <PetalBackground />
    
    <!-- 主内容 -->
    <AppHeader />
    <AppSidebar />
    <main>
      <!-- 文章内容或实验室 -->
    </main>
    
    <!-- 全局组件 -->
    <MusicPlayer />
    <SearchModal />
  </div>
</template>
```

## 3. 组件目录结构

### 3.1 components/ 目录

```
components/
├── AppHeader.vue       # 顶部导航栏
├── AppSidebar.vue      # 侧边文件树
├── ArticleCard.vue     # 文章卡片
├── FileTree.vue        # 递归文件树组件
├── SearchModal.vue     # 搜索弹窗
├── MusicPlayer.vue     # 音乐播放器
├── WriteEditor.vue     # Markdown 编辑器
│
├── lab/                # 实验室组件
│   ├── LabDashboard.vue     # 实验室主控制面板
│   ├── stage1-foundation/   # 第1阶段：基础
│   ├── stage2-js-basics/    # 第2阶段：JS基础
│   └── ...
│
└── petal/              # 花瓣动画
    └── usePetals.ts
```

### 3.2 组件命名规范

- **PascalCase**: 组件文件名使用大驼峰（AppHeader.vue）
- **功能前缀**: Lab 开头表示实验室组件
- **单一职责**: 每个组件只做一件事

## 4. composables/ 组合式函数

```
composables/
├── index.ts            # 统一导出
├── useFile.ts          # 文件操作
├── useSearch.ts        # 搜索功能
├── useMarkdown.ts      # Markdown 渲染
├── useLightbox.ts      # 图片灯箱
├── useWallpapers.ts    # 壁纸管理
├── useGitHubPublish.ts # GitHub 发布
└── useContentClick.ts  # 内容点击处理
```

**命名约定**: 以 `use` 开头，表示这是一个 Composable。

## 5. stores/ 状态管理

```
stores/
├── index.ts           # 统一导出
├── appStore.ts        # 全局应用状态（主题、语言）
├── articleStore.ts    # 文章相关状态
├── musicStore.ts      # 音乐播放器状态
└── learningStore.ts   # 学习进度状态
```

## 6. 配置文件

### 6.1 vite.config.ts

```typescript
// 📍 vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': '/src'  // 路径别名
    }
  }
})
```

### 6.2 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "strict": true,
    "jsx": "preserve",
    "moduleResolution": "node"
  },
  "include": ["**/*.ts", "**/*.vue"]
}
```

---

## 🧪 动手实验

本章节对应实验室：**LabCodeEvolution**, **LabBuildTools**

完成以下练习：
1. [ ] 在实验室中查看项目从 HTML 到 Vue 的演进
2. [ ] 理解 Vite 的模块热更新原理
3. [ ] 修改 `constants.ts` 中的翻译，观察实时更新

---

## 📚 关键知识点

| 目录/文件 | 作用 |
|-----------|------|
| `index.html` | 唯一的 HTML 入口 |
| `App.vue` | 组件树的根 |
| `components/` | UI 组件库 |
| `composables/` | 可复用逻辑 |
| `stores/` | 全局状态 |
| `public/` | 静态资源（不经过构建） |
