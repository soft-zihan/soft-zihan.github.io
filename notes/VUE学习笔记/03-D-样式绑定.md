# 样式绑定

> **对应实验室**: LabClassStyle  
> **涉及源文件**: App.vue, components/*.vue

## 1. 绑定 Class

### 1.1 对象语法

```vue
<template>
  <!-- 对象语法：键是类名，值是布尔条件 -->
  <div :class="{ active: isActive, error: hasError }"></div>
  
  <!-- 当 isActive=true, hasError=false 时 -->
  <!-- 渲染为: <div class="active"></div> -->
</template>

<script setup>
import { ref } from 'vue'
const isActive = ref(true)
const hasError = ref(false)
</script>
```

### 1.2 数组语法

```vue
<template>
  <!-- 数组语法：包含多个类名 -->
  <div :class="[activeClass, errorClass]"></div>
  
  <!-- 数组中使用条件表达式 -->
  <div :class="[isActive ? 'active' : '', errorClass]"></div>
  
  <!-- 数组中嵌套对象 -->
  <div :class="[{ active: isActive }, errorClass]"></div>
</template>

<script setup>
const activeClass = ref('active')
const errorClass = ref('text-danger')
const isActive = ref(true)
</script>
```

### 1.3 与静态 class 合并

```vue
<template>
  <!-- 静态 class 和动态 :class 会合并 -->
  <div class="base-style" :class="{ active: isActive }"></div>
  
  <!-- 渲染为: <div class="base-style active"></div> -->
</template>
```

## 2. 本站实例：主题切换

```vue
<!-- 📍 App.vue - 暗黑模式 -->
<template>
  <div :class="{ dark: isDark }">
    <!-- 整个应用的根元素 -->
    <!-- 当 isDark 为 true 时，添加 dark 类 -->
    <!-- Tailwind CSS 会根据 dark 类应用暗色样式 -->
  </div>
</template>

<script setup>
import { useAppStore } from './stores/appStore'
const { isDark } = useAppStore()
</script>
```

```vue
<!-- 📍 ArticleCard.vue - 条件样式 -->
<template>
  <article 
    class="card"
    :class="{
      'card--featured': isFeatured,
      'card--read': isRead
    }"
  >
    ...
  </article>
</template>
```

## 3. 绑定 Style

### 3.1 对象语法

```vue
<template>
  <!-- 对象语法：键是 CSS 属性，值是对应的值 -->
  <div :style="{ color: activeColor, fontSize: fontSize + 'px' }"></div>
  
  <!-- 推荐使用 camelCase -->
  <div :style="{ backgroundColor: bgColor }"></div>
  
  <!-- 也支持 kebab-case（需要引号） -->
  <div :style="{ 'background-color': bgColor }"></div>
</template>

<script setup>
const activeColor = ref('red')
const fontSize = ref(16)
const bgColor = ref('#f0f0f0')
</script>
```

### 3.2 绑定样式对象

```vue
<template>
  <div :style="styleObject"></div>
</template>

<script setup>
const styleObject = reactive({
  color: 'red',
  fontSize: '14px',
  fontWeight: 'bold'
})
</script>
```

### 3.3 数组语法

```vue
<template>
  <!-- 合并多个样式对象 -->
  <div :style="[baseStyles, overrideStyles]"></div>
</template>

<script setup>
const baseStyles = { fontSize: '14px', color: 'blue' }
const overrideStyles = { color: 'red' }  // 会覆盖 color
// 结果: { fontSize: '14px', color: 'red' }
</script>
```

## 4. 本站实例：动态样式

### 4.1 主题颜色

```vue
<!-- 📍 使用 CSS 变量 -->
<template>
  <div 
    :style="{ 
      '--primary-color': themeColor,
      '--bg-color': isDark ? '#1a1a1a' : '#ffffff'
    }"
  >
    <button class="btn">按钮</button>
  </div>
</template>

<style>
.btn {
  background-color: var(--primary-color);
  color: var(--bg-color);
}
</style>
```

### 4.2 花瓣动画

```vue
<!-- 📍 PetalBackground.vue -->
<template>
  <div 
    v-for="petal in petals"
    :key="petal.id"
    class="petal"
    :style="{
      left: petal.x + '%',
      animationDuration: petal.duration + 's',
      animationDelay: petal.delay + 's',
      transform: `scale(${petal.scale})`
    }"
  ></div>
</template>
```

### 4.3 音乐进度条

```vue
<!-- 📍 MusicPlayer.vue -->
<template>
  <div class="progress-bar">
    <div 
      class="progress-fill"
      :style="{ width: progressPercent + '%' }"
    ></div>
  </div>
</template>

<script setup>
const progressPercent = computed(() => 
  (currentTime.value / duration.value) * 100
)
</script>
```

## 5. 自动前缀

Vue 会自动为需要浏览器前缀的 CSS 属性添加前缀：

```vue
<template>
  <!-- Vue 会自动添加 -webkit-transform 等前缀 -->
  <div :style="{ transform: 'rotate(45deg)' }"></div>
</template>
```

## 6. 多值样式

提供多个值作为数组，浏览器会使用支持的最后一个：

```vue
<template>
  <div :style="{ display: ['-webkit-box', '-ms-flexbox', 'flex'] }"></div>
</template>
```

## 7. 计算样式

```vue
<template>
  <div :class="cardClasses" :style="cardStyles">
    卡片内容
  </div>
</template>

<script setup>
import { computed, ref } from 'vue'

const isActive = ref(true)
const size = ref('large')
const bgColor = ref('#fff')

// 计算 class
const cardClasses = computed(() => ({
  'card': true,
  'card--active': isActive.value,
  [`card--${size.value}`]: true
}))

// 计算 style
const cardStyles = computed(() => ({
  backgroundColor: bgColor.value,
  padding: size.value === 'large' ? '20px' : '10px'
}))
</script>
```

---

## 🧪 动手实验

本章节对应实验室：**LabClassStyle**

完成以下练习：
1. [ ] 使用对象语法实现条件类名
2. [ ] 使用动态 style 实现主题切换
3. [ ] 结合计算属性管理复杂样式

---

## 📝 小结

| 语法 | 用途 | 示例 |
|------|------|------|
| `:class="{ active: bool }"` | 条件类名 | 对象语法 |
| `:class="[cls1, cls2]"` | 多个类名 | 数组语法 |
| `:style="{ color: val }"` | 内联样式 | 对象语法 |
| `:style="[obj1, obj2]"` | 合并样式 | 数组语法 |
