# 🚀 实验室激进重构方案 2.0

> **核心理念**：学习笔记 = 理论教材，实验室 = 可视化演示 + 交互练习  
> **目标**：以 **Sakura Notes 本身代码** 为唯一示例来源，打造「边学边做」的沉浸式前端学习体验  
> **时间**：2026年1月

---

## 🎯 一、设计哲学

### 1.1 「代码即教材」原则

```
传统模式:                    新模式:
┌──────────────┐           ┌──────────────────────────────────┐
│  通用示例代码 │           │     Sakura Notes 源码即教材       │
│  foo, bar    │    →      │  useSearch.ts → 学搜索功能        │
│  无实际意义   │           │  App.vue → 学组件结构             │
│              │           │  appStore.ts → 学状态管理          │
└──────────────┘           └──────────────────────────────────┘
```

### 1.2 「笔记 ↔ 实验室」双向映射

| 学习笔记 | 对应实验室 | 关联项目文件 |
|---------|-----------|-------------|
| 00-A-JavaScript基础语法 | LabJsBasics | App.vue, useSearch.ts |
| 00-B-CSS基础与Tailwind | LabCssBasics + LabCssLayout | WallpaperLayer.vue, AppSidebar.vue |
| 01-基础概念与MVVM | LabCodeEvolution + LabHtmlBasics | index.html, index.tsx, App.vue |
| 02-响应式变量Ref | LabReactivity | appStore.ts, musicStore.ts |
| 03-指令(Directives) | LabDirectives | App.vue (v-if/v-show/v-for) |
| 04-A-事件处理 | LabEventHandling | AppHeader.vue, SearchModal.vue |
| 04-B-插槽系统 | LabSlot | AppSidebar.vue, ArticleCard.vue |
| 04-组件通信 | LabPropsEmit | FileTree.vue, FolderView.vue |
| 05-生命周期 | LabLifecycle | useSearch.ts (onMounted), App.vue |

---

## 📚 二、学习笔记激进重构

### 2.1 新的笔记结构（按学习顺序）

```
notes/VUE学习笔记/
├── 00-前置基础/
│   ├── 00-A-JavaScript基础语法.md      ✅ 已创建，需扩充
│   ├── 00-B-CSS基础与Tailwind.md       ✅ 已创建，需扩充
│   └── 00-C-TypeScript快速入门.md      🆕 新增
│
├── 01-Vue入门/
│   ├── 01-A-项目结构解析.md            🔄 重构自原01，以本站为例
│   ├── 01-B-模板语法与插值.md          🆕 新增
│   └── 01-C-SFC单文件组件.md           🆕 新增
│
├── 02-响应式核心/
│   ├── 02-A-ref与reactive.md           🔄 重构
│   ├── 02-B-computed计算属性.md        🆕 新增
│   └── 02-C-watch侦听器.md             🆕 新增
│
├── 03-指令系统/
│   ├── 03-A-条件渲染(v-if v-show).md   🔄 拆分
│   ├── 03-B-列表渲染(v-for).md         🆕 新增
│   ├── 03-C-表单绑定(v-model).md       🆕 新增
│   └── 03-D-样式绑定(class style).md   🆕 新增
│
├── 04-组件进阶/
│   ├── 04-A-事件处理.md                ✅ 已有
│   ├── 04-B-插槽系统.md                ✅ 已有
│   ├── 04-C-Props与Emit.md             🔄 重构
│   └── 04-D-组件设计模式.md            🆕 新增
│
├── 05-生命周期与Composables/
│   ├── 05-A-生命周期钩子.md            🔄 重构
│   ├── 05-B-Composables基础.md         🆕 新增（结合useSearch等）
│   └── 05-C-自定义Hooks实战.md         🆕 新增
│
└── 06-状态管理与路由/
    ├── 06-A-Pinia状态管理.md           🆕 新增（结合appStore）
    └── 06-B-SPA路由原理.md             🆕 新增
```

### 2.2 笔记模板规范

每篇笔记必须包含以下结构：

```markdown
# XX. 章节标题

> **学习目标**: 一句话概括
> **对应实验室**: [LabXxx →](#)
> **涉及源文件**: App.vue, useSearch.ts, ...

## 1. 概念讲解
理论知识，配合项目代码说明

## 2. 源码剖析
```typescript
// 📍 文件: /composables/useSearch.ts (行 15-30)
// 这里展示的是 Sakura Notes 的真实代码
export function useSearch() {
  const searchQuery = ref('')  // ← ref 创建响应式变量
  // ...
}
```

## 3. 动手实验
👉 **打开实验室 [LabXxx]** 完成以下练习：
- [ ] 练习1：...
- [ ] 练习2：...

## 4. 扩展阅读
- MDN 链接
- Vue 官方文档
```

---

## 🧪 三、实验室激进重构

### 3.1 新的实验室架构（8阶段）

```
🏠 实验室首页 (LabDashboard)
│
├── 第1阶段：起源与基础 (Origin)
│   ├── LabCodeEvolution    代码演进史 → 对应笔记01-A
│   ├── LabHtmlBasics       HTML实战 → 本站index.html
│   └── LabBrowserEngine    🆕 浏览器渲染原理
│
├── 第2阶段：JavaScript核心 (JS Core)
│   ├── LabJsBasics         变量/函数/数组 → useSearch示例 ✅
│   ├── LabJsAdvanced       🆕 闭包/作用域/this → App.vue回调
│   ├── LabDom              DOM操作 → useContentClick
│   └── LabAsync            🆕 Promise/async → useFile示例
│
├── 第3阶段：CSS全栈 (CSS Mastery)
│   ├── LabCssBasics        选择器/盒模型 ✅
│   ├── LabCssLayout        Flex/Grid ✅
│   ├── LabCssAnimation     🆕 动画/过渡 → PetalBackground
│   └── LabTailwind         Tailwind实战
│
├── 第4阶段：TypeScript (Type Safety)
│   ├── LabTypeScript       TS基础 → types.ts示例
│   └── LabTsAdvanced       🆕 泛型/类型体操 → composables类型
│
├── 第5阶段：工程化 (Engineering)
│   ├── LabModuleSystem     ES Modules
│   ├── LabNpm              包管理
│   ├── LabVite             🆕 Vite深入 → vite.config.ts
│   └── LabBuildOptimize    🆕 构建优化
│
├── 第6阶段：Vue 3核心 (Vue Core)
│   ├── LabReactivity       响应式 → appStore实战
│   ├── LabDirectives       指令系统 → App.vue
│   ├── LabEventHandling    事件处理
│   ├── LabVueList          列表渲染 → FileTree
│   └── LabLifecycle        生命周期
│
├── 第7阶段：Vue 3进阶 (Vue Advanced)
│   ├── LabPropsEmit        组件通信
│   ├── LabSlot             插槽系统
│   ├── LabComposables      🆕 Composables → useSearch剖析
│   ├── LabPinia            🆕 Pinia状态 → appStore/musicStore
│   └── LabProvideInject    🆕 依赖注入
│
└── 第8阶段：综合挑战 (Challenge)
    ├── LabQuizGame         测验游戏
    ├── LabMiniProject      🆕 迷你项目：复刻本站功能
    └── LabCodeReview       🆕 代码审查：本站源码
```

### 3.2 实验室设计原则

#### 原则1：每个实验室必须关联真实代码

```typescript
// ❌ 旧方式：抽象示例
const count = ref(0)  // 这有什么用？

// ✅ 新方式：本站真实代码
// 📍 来自 /stores/appStore.ts
const isDark = ref(false)          // 控制深色模式
const showParticles = ref(true)    // 控制花瓣效果
const userSettings = ref({...})    // 用户设置持久化
```

#### 原则2：交互演示与笔记内容一一对应

```
笔记章节                    实验室演示
─────────────────────────────────────────────
02-A 讲 ref 原理      →    LabReactivity 可视化
                          - 展示 appStore.isDark 变化
                          - 实时修改主题，观察响应式
                          
05-B 讲 Composables   →    LabComposables 交互
                          - useSearch 代码逐行解析
                          - 在演示区调用真实搜索功能
```

#### 原则3：渐进式难度曲线

```
入门 ──────────────────────────────────── 高级
  │                                        │
  ├── HTML标签认识                         │
  ├── JS变量是什么                         │
  ├── CSS选择器                            │
  ├──────── Flex布局 ─────────             │
  ├──────── Vue模板 ──────────             │
  ├──────────── Composables ───            │
  └──────────────── 状态管理 ──────────────┘
```

---

## 🆕 四、新增实验室详细设计

### 4.1 LabBrowserEngine（浏览器渲染原理）

**目的**：解释浏览器如何将 HTML/CSS/JS 变成页面

**交互内容**：
1. **解析流程动画**：HTML → DOM Tree → CSSOM → Render Tree → Paint
2. **本站实例**：展示 `index.html` 如何被解析
3. **Performance 模拟**：阻塞渲染演示

**关联笔记**：01-A-项目结构解析

---

### 4.2 LabJsAdvanced（JavaScript进阶）

**目的**：深入理解闭包、作用域、this绑定

**交互内容**：
```typescript
// 本站实例：App.vue 中的事件回调
const handleSidebarFileSelect = (file: FileNode) => {
  // 这里的 this 是什么？为什么用箭头函数？
  sidebarOpen.value = false  // 闭包：访问外部作用域
}
```

**演示区**：
- 作用域链可视化
- this 指向交互式诊断器
- 闭包内存引用图

---

### 4.3 LabAsync（异步编程）

**目的**：Promise、async/await、错误处理

**本站实例来源**：
```typescript
// 📍 composables/useFile.ts
async function fetchFileContent(file: FileNode) {
  try {
    const response = await fetch(`/notes/${file.path}`)
    if (!response.ok) throw new Error('Failed to fetch')
    return await response.text()
  } catch (e) {
    console.error(e)
  }
}
```

**交互内容**：
1. Promise 状态机动画
2. async/await 执行顺序可视化
3. 错误处理最佳实践

---

### 4.4 LabCssAnimation（CSS动画）

**目的**：transition、animation、keyframes

**本站实例来源**：
```css
/* 📍 components/PetalBackground.vue */
@keyframes float {
  0%, 100% { transform: translateY(0) rotate(0deg); }
  50% { transform: translateY(-20px) rotate(180deg); }
}

.petal {
  animation: float 8s ease-in-out infinite;
}
```

**交互内容**：
1. 花瓣动画参数实时调节
2. CSS vs JS 动画对比
3. 性能优化（will-change, GPU加速）

---

### 4.5 LabComposables（Vue Composables）

**目的**：理解组合式函数的设计思想

**本站实例剖析**：
```typescript
// 📍 完整剖析 useSearch.ts
export function useSearch(fetchFileFn) {
  // 1. 状态定义（响应式）
  const searchQuery = ref('')
  const searchResults = ref<SearchResult[]>([])
  
  // 2. 计算属性
  const hasResults = computed(() => searchResults.value.length > 0)
  
  // 3. 副作用（生命周期）
  onMounted(() => initSearchIndex())
  
  // 4. 方法
  const search = async (query: string) => {...}
  
  // 5. 返回公共API
  return { searchQuery, searchResults, hasResults, search }
}
```

**交互内容**：
1. Composable 结构拆解动画
2. 对比 Vue 2 Mixins vs Vue 3 Composables
3. 实时调用 useSearch，观察内部状态

---

### 4.6 LabPinia（Pinia状态管理）

**目的**：理解全局状态管理

**本站实例**：
```typescript
// 📍 stores/appStore.ts 完整剖析
export const useAppStore = defineStore('app', () => {
  // State
  const isDark = ref(false)
  const lang = ref<'en' | 'zh'>('zh')
  
  // Getters (computed)
  const t = computed(() => I18N[lang.value])
  
  // Actions
  function toggleTheme() {
    isDark.value = !isDark.value
  }
  
  return { isDark, lang, t, toggleTheme }
})
```

**交互内容**：
1. DevTools 风格状态检查器
2. 实时修改 appStore，观察全站变化
3. musicStore 对比学习

---

### 4.7 LabMiniProject（迷你项目）

**目的**：综合应用所学，复刻本站功能

**挑战任务**：
```
🎯 任务1：复刻主题切换
   - 实现 isDark toggle
   - 持久化到 localStorage
   - 应用 Tailwind dark 类

🎯 任务2：复刻搜索功能
   - 实现 useSearch composable
   - 集成 MiniSearch 库
   - 搜索结果高亮

🎯 任务3：复刻文件树
   - 递归组件
   - 展开/折叠状态
   - Props + Emit 通信
```

---

## 📝 五、学习笔记详细重构

### 5.1 新增笔记内容规划

#### 00-C-TypeScript快速入门.md

```markdown
# TypeScript 快速入门

> **对应实验室**: LabTypeScript
> **涉及源文件**: types.ts, composables/*.ts

## 1. 为什么需要 TypeScript？

本站的类型定义 (`types.ts`):
```typescript
export interface FileNode {
  name: string
  path: string
  type: NodeType
  children?: FileNode[]  // 可选属性
}
```

## 2. 基础类型注解

来自 `appStore.ts`:
```typescript
const lang = ref<'en' | 'zh'>('zh')  // 字面量联合类型
```
```

#### 02-B-computed计算属性.md

```markdown
# Computed 计算属性

> **对应实验室**: LabReactivity
> **涉及源文件**: appStore.ts, composables/useSearch.ts

## 1. 什么是计算属性？

本站的国际化翻译（`appStore.ts`）:
```typescript
const lang = ref<'en' | 'zh'>('zh')
const t = computed(() => I18N[lang.value])  // 自动追踪 lang 变化
```

## 2. computed vs 普通函数
...
```

#### 05-B-Composables基础.md

```markdown
# Composables 组合式函数

> **对应实验室**: LabComposables
> **涉及源文件**: composables/ 目录全部

## 1. 什么是 Composable？

本站 composables 目录结构:
```
composables/
├── useSearch.ts      搜索功能封装
├── useFile.ts        文件操作封装
├── useLightbox.ts    图片灯箱
├── useMarkdown.ts    Markdown 渲染
└── useWallpapers.ts  壁纸管理
```

## 2. 剖析 useSearch.ts

### 2.1 状态管理
```typescript
const searchQuery = ref('')
const searchResults = ref<SearchResult[]>([])
```

### 2.2 生命周期集成
```typescript
onMounted(() => {
  initSearchIndex(fileSystem.value, currentLang.value)
})
```
```

---

## 🔗 六、笔记与实验室联动机制

### 6.1 在笔记中添加实验室入口

每篇笔记底部添加：

```markdown
---
## 🧪 动手实验

本章节对应实验室：**[LabReactivity →](/lab/reactivity)**

完成以下交互练习：
1. [ ] 修改 `isDark` 观察主题切换
2. [ ] 修改 `lang` 观察语言切换
3. [ ] 查看 `computed` 依赖追踪
```

### 6.2 在实验室中添加笔记链接

每个实验室顶部添加：

```vue
<div class="bg-blue-50 dark:bg-blue-900/20 rounded-xl p-4 mb-6">
  <span class="text-sm">📖 配套笔记：</span>
  <a href="/notes/VUE学习笔记/02-A-ref与reactive.md" class="text-blue-500 hover:underline">
    02-A-ref与reactive
  </a>
</div>
```

### 6.3 代码高亮同步

实验室中展示的代码片段，自动链接到源文件：

```vue
<CodeBlock 
  file="/composables/useSearch.ts" 
  :lines="[15, 30]"
  :highlight="[18, 22]"
  linkToSource
/>
```

---

## 📊 七、实施计划

### 第一期：基础补全（1-2周）

| 任务 | 优先级 | 工作量 |
|------|-------|--------|
| 重构 LabJsBasics，更多本站示例 | P0 | 2天 |
| 新建 LabAsync（异步编程）| P0 | 3天 |
| 新建 LabCssAnimation（CSS动画）| P1 | 2天 |
| 更新笔记 00-A/00-B，扩充内容 | P1 | 2天 |

### 第二期：Vue进阶（2-3周）

| 任务 | 优先级 | 工作量 |
|------|-------|--------|
| 新建 LabComposables（核心）| P0 | 4天 |
| 新建 LabPinia | P0 | 3天 |
| 新建笔记 05-B/06-A | P1 | 3天 |
| 重构 LabReactivity，加入 appStore 示例 | P1 | 2天 |

### 第三期：综合挑战（1-2周）

| 任务 | 优先级 | 工作量 |
|------|-------|--------|
| 新建 LabMiniProject | P0 | 5天 |
| 完善笔记 ↔ 实验室双向链接 | P1 | 2天 |
| 添加学习路径引导 | P2 | 1天 |

---

## 🎨 八、UI/UX 增强

### 8.1 学习进度追踪

```typescript
// 新增 stores/learningStore.ts
export const useLearningStore = defineStore('learning', () => {
  const completedLabs = ref<string[]>([])
  const completedNotes = ref<string[]>([])
  const currentStage = computed(() => {
    // 根据完成情况计算当前阶段
  })
})
```

### 8.2 代码溯源功能

在实验室中点击代码片段，可以：
- 跳转到 GitHub 源文件
- 在侧边栏高亮对应文件
- 显示代码上下文

### 8.3 实时代码编辑器

集成 Monaco Editor，允许用户：
- 修改本站组件代码
- 实时预览效果
- 保存到本地实验

---

## 🎯 九、成功指标

| 指标 | 目标 |
|------|-----|
| 学习笔记篇数 | 20+ 篇 |
| 实验室组件数 | 25+ 个 |
| 每个实验室本站代码示例 | ≥3 个 |
| 笔记 ↔ 实验室覆盖率 | 100% |
| 学习路径完整性 | 从零基础到独立开发 |

---

## 📋 十、文件变更清单

### 新增文件

```
components/
├── LabBrowserEngine.vue      🆕
├── LabJsAdvanced.vue         🆕
├── LabAsync.vue              🆕
├── LabCssAnimation.vue       🆕
├── LabTsAdvanced.vue         🆕
├── LabVite.vue               🆕
├── LabBuildOptimize.vue      🆕
├── LabComposables.vue        🆕
├── LabPinia.vue              🆕
├── LabProvideInject.vue      🆕
├── LabMiniProject.vue        🆕
└── LabCodeReview.vue         🆕

stores/
└── learningStore.ts          🆕

notes/VUE学习笔记/
├── 00-C-TypeScript快速入门.md     🆕
├── 01-B-模板语法与插值.md         🆕
├── 01-C-SFC单文件组件.md          🆕
├── 02-B-computed计算属性.md       🆕
├── 02-C-watch侦听器.md            🆕
├── 03-A-条件渲染.md               🔄
├── 03-B-列表渲染.md               🆕
├── 03-C-表单绑定.md               🆕
├── 03-D-样式绑定.md               🆕
├── 04-D-组件设计模式.md           🆕
├── 05-B-Composables基础.md        🆕
├── 05-C-自定义Hooks实战.md        🆕
├── 06-A-Pinia状态管理.md          🆕
└── 06-B-SPA路由原理.md            🆕
```

### 修改文件

```
components/
├── LabDashboard.vue          🔄 重构8阶段结构
├── LabJsBasics.vue           🔄 扩充本站示例
├── LabReactivity.vue         🔄 加入appStore实例
└── LabTypeScript.vue         🔄 加入types.ts示例

constants.ts                  🔄 新增i18n字符串
README.md                     🔄 更新文档
```

---

> **总结**：这份方案的核心是「**让学习者在学习 Vue 的同时，完全理解一个真实项目的代码**」。不再是 foo/bar 的抽象示例，而是「为什么 Sakura Notes 这样写」的实战解析。
